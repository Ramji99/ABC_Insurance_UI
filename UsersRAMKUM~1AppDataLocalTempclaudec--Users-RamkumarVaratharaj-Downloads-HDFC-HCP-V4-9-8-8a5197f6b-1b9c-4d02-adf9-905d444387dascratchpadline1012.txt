| 80 | v2.8.2 | 2026-08-27 | **Added Barcode to the Scan Tag list search's match scope and set its placeholder to the user's exact requested text.** `getFilteredExcludingStatusChip()` in `app.js` (Iteration 78's `[e.inwardId, e.claimId, policyNum]`) gained `e.barcode` — `[e.inwardId, e.claimId, policyNum, e.barcode]` — since `entries[]` (unlike the Payment dashboard's datasets touched in Iteration 79) genuinely has a `barcode` field, so all four identifiers the user named are now real, matched fields, not an aspirational list applied partially. `#listSearch`'s placeholder in `index.html` set to the literal text the user specified: **"Search by Inward ID, Claim ID, Policy Number, Barcode."** (previously "Search by Inward IDs, Claim Numbers, or Policy Numbers..." from Iteration 78, which both omitted Barcode and used different wording/punctuation). Verified via `server.js` + headless Chromium/Playwright: placeholder matches exactly; searching a real record's barcode value now returns 1 row (previously wouldn't have matched at all); searching that same record's patient name still correctly returns 0 rows, confirming the Iteration 78 exclusion held through this addition. Zero console errors | app.js, index.html, AGENTS.md | User |